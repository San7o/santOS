* Kernel Build System
Run make with one of the following options:
  
  all       used by default, builds the selected projects
  config    generate config.sh to configure the build system
  iso       generate an iso after a kernel is built
  qemu      run qemu emulator to test the kernel
  clean     clean the build files
  help      show this message
